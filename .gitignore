# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Log/OS Files
*.log
.DS_Store
*.swp

# Android Studio generated files and folders
captures/
.externalNativeBuild/
.cxx/
*.aab
*.apk
*.ap_
*.dex
output-metadata.json

# IntelliJ
*.iml
.idea/
.kotlin/

# Signing: losing or leaking these means friends must reinstall to update
*.jks
*.keystore
keystore.properties
signing.properties

# Python importer
__pycache__/
*.pyc
.pytest_cache/
.venv/

# --- Personal training data: stays on this machine, never pushed ---
# The repo is public. text_log is the source of truth and out/ is derived
# from it; both are backed up to the NAS instead of to git.
/text_log
/out/

# Seed data for debug builds only: personal, and never in a release APK
/app/src/debug/assets/
